{
    "name": "node-react",
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && npm start",
        "server": "cd server && nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
        "test-server": "cd server && jest --coverage",
        "test-client": "cd client && jest --coverage"
    },
    "devDependencies": {
        "concurrently": "^3.5.0",
        "eslint": "^8.33.0",
        "jest": "^29.4.2",
        "nodemon": "^2.0.20"
    }
}
